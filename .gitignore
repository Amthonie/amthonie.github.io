# Local-only Claude Code notes (kept out of this public repo)
CLAUDE.md

# JetBrains IDEs
.idea/

# macOS
.DS_Store

# Editor / OS cruft
*.swp
Thumbs.db

# Build tooling / output (CSS is rebuilt in CI; _site is CI-only)
node_modules/
_site/
